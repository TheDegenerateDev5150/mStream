## WIP
- Add in torrenting support - claude has a plan
- ffmpeg builds for alpine linux
- localzation - on branch 

## Also do

- Add in docker examples
- edit file operations
- add docker files based on local setup
- make API docs

## The prompt

Ok Claude, we're going to make an ecommerce store. BUT, it's going to be the best ecommerce software ever made. We're going to be selling supplements, so that might affect the API. 

First, it's going to be written in Rust for performance reasons.  Build all the API endpoints necessary to get up and running.

The real crazy part is we're going to use SQLite as a DB. Sepcifically, we're going to use an implementation of the RAFT algorithm. Lookup current implementation and choose the one that is easiest to be configured.

And that's the real crux of the problem. I want be able get this up an running effortlessly. I want to get a cluster of old computers, have each one running an instance of the program, and have it all work automatically. If one computer goes down, there no issue because our DB uses RAFT.

Focus on getting it working on multiple PCs with minimal configuration. 




###

Can we add the ability for the the rust audio player to drive LED lights if it's running on a Raspberry pi (or anything with GRPIO pins). I've seen some projects like this, and I think it would be cool the build into the rust player to make a flashy tech demo.

Make all this code optional and not load by default. 



